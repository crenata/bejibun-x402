{
    "name": "@bejibun/x402-benchmarks",
    "private": true,
    "type": "module",
    "description": "Speed comparison: baseline (previously published npm release) vs the optimized @bejibun/x402 in this repo. Run with `bun run`.",
    "scripts": {
        "install-deps": "bun install --cwd .. --no-save \"@bejibun-baseline/x402@npm:@bejibun/x402@0.2.11\"",
        "coldstart": "bun run scripts/coldstart.mjs && prettier --write . --log-level=silent",
        "throughput": "bun run scripts/throughput.mjs && prettier --write . --log-level=silent",
        "bench": "bun run install-deps && bun run coldstart && bun run throughput"
    }
}
